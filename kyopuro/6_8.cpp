#include<bits/stdc++.h>
using namespace std;

const long long INF = 1LL << 60;

int main(void) {
	int N;
	cin >> N;
	vector<long long> H(N),S(N);
	for (int i = 0; i < N; i++) {
		cin >> H[i] >> S[i];
	}

	long long left = 0;
	long long right = INF;
	while (right - left > 1) {

	}

}